name: Create User
method: POST
url: $base_url/api/v1/auth/create/
timeout: 0
followRedirects: true
maxRedirects: 5
body: |-
  {
    "email": "$email",
    "password": "$password",
    "password2": "$password2"
  }
body_type: json
auth:
  type: bearer
  token: $token
